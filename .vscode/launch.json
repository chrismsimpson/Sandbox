{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run",   
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/src/console",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}